# 后端外网反向代理配置完成

**配置时间**: 2026-01-10

---

## ✅ 配置完成

### 网络地址映射

| 服务 | 内网地址 | 外网地址 | 状态 |
|------|----------|----------|------|
| **前端** | 192.168.214.102:3000 | 47.120.47.251:3002 | ✅ |
| **后端** | 192.168.214.102:8000 | 47.120.47.251:3003 | ✅ |

---

## 📝 完成的配置

### 1. 环境变量配置

**文件**: `.env.local` (已创建)
```bash
AGENT_URL=http://47.120.47.251:3003
```

### 2. 网络配置文档

**文件**: `NETWORK_CONFIG.md` (已更新)
- 添加后端外网地址: `47.120.47.251:3003`
- 更新端口映射表
- 添加环境变量配置说明

### 3. 后端服务配置

**文件**: `agent/main.py`
```python
if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)  # ✅ 允许外部访问
```

**当前状态**: 后端监听 `0.0.0.0:8000` ✅

---

## 🔄 外网访问流程

```
用户浏览器
    ↓
47.120.47.251:3002 (前端代理)
    ↓
Next.js 读取 AGENT_URL 环境变量
    ↓
http://47.120.47.251:3003 (后端代理)
    ↓
192.168.214.102:8000 (后端服务)
    ↓
LangGraph Agent
```

---

## ⚠️ 需要配置的反向代理规则

**请在反向代理服务器上添加以下规则**:

```nginx
# 后端外网反向代理
location / {
    proxy_pass http://192.168.214.102:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # 支持 SSE (Server-Sent Events) 用于 AG-UI 协议
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 86400s;
    proxy_connect_timeout 86400s;
}
```

**端口映射**:
```
47.120.47.251:3003 → 192.168.214.102:8000
```

---

## 🧪 测试步骤

### 1. 测试反向代理配置

```bash
# 测试外网后端端口
curl http://47.120.47.251:3003

# 应该返回 AG-UI 协议响应或 Method Not Allowed (405)
```

### 2. 测试前端外网访问

```bash
# 测试外网前端
curl http://47.120.47.251:3002

# 在浏览器中打开并测试:
# 1. 上传 PDF 文件
# 2. 发送消息给 Agent
# 3. 检查 4 个面板是否更新
```

### 3. 检查服务状态

```bash
# 检查后端监听
lsof -i :8000

# 应该看到: python3 *:8000 (LISTEN)
```

---

## 📊 配置文件清单

- [x] `.env.local` - 环境变量配置 (已创建)
- [x] `NETWORK_CONFIG.md` - 网络配置文档 (已更新)
- [x] `agent/main.py` - 后端监听配置 (已验证)
- [ ] **反向代理规则** - 需要在代理服务器上配置

---

## 🎯 下一步

1. **配置反向代理**: 在反向代理服务器上添加 `47.120.47.251:3003 → 192.168.214.102:8000` 规则
2. **测试外网访问**: 访问 `http://47.120.47.251:3002` 并测试完整功能
3. **验证 Agent 交互**: 上传文件并与 Agent 对话,确认状态同步正常

---

## ✅ 配置完成清单

- [x] 确认后端外网端口: `47.120.47.251:3003`
- [x] 创建 `.env.local` 配置文件
- [x] 更新 `NETWORK_CONFIG.md` 文档
- [x] 验证后端监听 `0.0.0.0:8000`
- [ ] 配置反向代理规则 (需要在外网代理服务器上操作)
- [ ] 测试外网访问完整流程

---

**状态**: ✅ 代码配置完成,等待反向代理配置

**访问地址**:
- 内网: http://192.168.214.102:3000
- 外网: http://47.120.47.251:3002
