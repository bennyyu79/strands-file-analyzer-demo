# 反向代理测试指南

## 🧪 测试反向代理是否生效

### 方法 1: 从外部测试 (推荐)

在任意可以访问外网的机器上执行:

```bash
# 测试后端外网地址
curl -v http://47.120.47.251:3003/

# 应该看到:
# HTTP/1.1 405 Method Not Allowed  ← 这个说明服务正常!
# 或者
# HTTP/1.1 200 OK
```

**关键**: 如果收到任何响应 (405、200、422等),说明反向代理**已生效**。

### 方法 2: 从内网服务器测试

在反向代理服务器 (192.168.214.102) 上执行:

```bash
# 1. 测试内网后端是否正常
curl http://192.168.214.102:8000/

# 2. 查看端口监听
netstat -tlnp | grep :8000

# 3. 测试防火墙
telnet 127.0.0.1 8000
```

### 方法 3: 浏览器测试 (最简单)

1. 打开浏览器
2. 访问: http://47.120.47.251:3003
3. 如果看到:
   - ✅ 任何响应 (包括错误信息) = 反向代理**工作正常**
   - ❌ 连接超时/无法访问 = 反向代理**未配置或有问题**

---

## 🔍 检查反向代理配置

### 如果使用 Nginx

检查配置文件 (通常在 `/etc/nginx/conf.d/` 或 `/etc/nginx/sites-available/`):

```bash
# 查看配置
cat /etc/nginx/conf.d/*.conf | grep -A 10 "3003"

# 应该看到类似:
# location / {
#     proxy_pass http://192.168.214.102:8000;
#     ...
# }
```

重载配置:
```bash
sudo nginx -t              # 测试配置
sudo nginx -s reload        # 重载配置
```

### 如果使用其他代理

请提供代理软件名称,我可以提供具体的检查方法。

---

## 📊 预期结果

### ✅ 反向代理生效
```bash
$ curl http://47.120.47.251:3003/
{"detail":"Method Not Allowed"}  ← 这个是正常的!
```

### ❌ 反向代理未生效
```bash
$ curl http://47.120.47.251:3003/
curl: (7) Failed to connect  ← 连接超时
```

---

## 🎯 快速判断

**如果浏览器访问 http://47.120.47.251:3002 (前端) 正常**:
- 前端反向代理 ✅ 已配置

**如果浏览器访问 http://47.120.47.251:3003 (后端)**:
- 有响应 → 后端反向代理 ✅ 已配置
- 超时/无法访问 → 后端反向代理 ❌ 未配置

---

请告诉我测试结果! 🚀
