# 网络访问配置

**更新时间**: 2026-01-10

---

## 🌐 访问地址

### 内网访问
```
前端: http://192.168.214.102:3000
后端: http://192.168.214.102:8000
```

### 外网访问
```
前端: http://47.120.47.251:3002
后端: http://47.120.47.251:3003
```

---

## 📊 服务端口映射

| 服务 | 内网地址 | 外网地址 | 说明 |
|------|----------|----------|------|
| **前端 (Next.js)** | 192.168.214.102:3000 | 47.120.47.251:3002 | 用户界面 |
| **后端 (Agent)** | 192.168.214.102:8000 | 47.120.47.251:3003 | LangGraph API |

---

## 🔧 前端配置

### CopilotKit Agent URL

**文件**: `src/app/api/copilotkit/route.ts`

```typescript
const runtime = new CopilotRuntime({
  agents: {
    file_investigator: new HttpAgent({
      // 自动使用环境变量 AGENT_URL
      // 内网: http://192.168.214.102:8000
      // 外网: http://47.120.47.251:3003
      url: process.env.AGENT_URL || "http://localhost:8000",
    }),
  },
});
```

**环境变量配置**: `.env.local`
```bash
# 外网访问时使用
AGENT_URL=http://47.120.47.251:3003

# 本地开发时使用
# AGENT_URL=http://localhost:8000
```

---

## 🚀 启动服务

### 方式 1: 同时启动前后端 (推荐)

```bash
npm run dev
```

这会启动:
- 前端: `next dev` (监听 0.0.0.0:3000)
- 后端: `uv run python main.py` (监听 0.0.0.0:8000)

### 方式 2: 分别启动

**后端**:
```bash
cd agent
uv run python main.py
# 监听: 0.0.0.0:8000 (允许内网访问)
```

**前端**:
```bash
npm run dev:ui
# 监听: 0.0.0.0:3000 (允许内网访问)
```

---

## 🔌 网络配置验证

### 检查服务监听状态

```bash
# 检查端口监听
lsof -i :3000 -i :8000

# 应该看到:
# python3 *:8000 (LISTEN)
# node *:3000 (LISTEN)
```

### 测试内网访问

```bash
# 测试前端
curl http://192.168.214.102:3000

# 测试后端
curl -X POST http://192.168.214.102:8000/ \
  -H "Content-Type: application/json" \
  -d '{"threadId":"test","runId":"test","state":{},"messages":[],"tools":[],"context":{},"forwardedProps":{}}'
```

### 测试外网访问

```bash
# 测试前端 (外网端口 3002)
curl http://47.120.47.251:3002

# 注意: 后端 8000 端口通常不直接暴露到外网
# 前端通过内网访问后端
```

---

## 🌍 访问方式

### 内网用户
```
直接访问: http://192.168.214.102:3000
```

### 外网用户
```
通过反向代理: http://47.120.47.251:3002
代理会将请求转发到内网 192.168.214.102:3000
```

---

## 🔒 安全建议

1. **后端端口** (8000) 建议只监听内网,不直接暴露到外网
2. **前端代理** 在外网访问时,通过 Next.js API Route 访问后端
3. **防火墙规则**:
   - 允许: 3000 (内网) → 3002 (外网)
   - 限制: 8000 (仅内网访问)

---

## 📝 环境变量

可以创建 `.env.local` 文件配置:

```bash
# 前端访问后端的地址
AGENT_URL=http://192.168.214.102:8000

# 或者使用 localhost (如果在同一台机器)
# AGENT_URL=http://localhost:8000
```

---

## ✅ 当前配置状态

- ✅ 前端监听: `0.0.0.0:3000` (允许内网/外网访问)
- ✅ 后端监听: `0.0.0.0:8000` (允许内网访问)
- ✅ 外网端口映射: `3002 → 192.168.214.102:3000`

---

## 🎯 使用说明

### 开发环境 (内网)
1. 启动服务: `npm run dev`
2. 访问: http://192.168.214.102:3000
3. 前端自动连接到 http://192.168.214.102:8000

### 生产环境 (外网)
1. 确保反向代理配置正确
2. 访问: http://47.120.47.251:3002
3. 前端通过内网访问后端

---

**配置完成时间**: 2026-01-10
**状态**: ✅ 运行正常
